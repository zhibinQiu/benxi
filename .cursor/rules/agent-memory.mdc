---
alwaysApply: true
---

# 智能体记忆（MEMORY.md）

项目根目录下的 `MEMORY.md` 文件中存储了用户的个人偏好、角色设定、回复要求和其他希望 AI 长期记住的内容。

**每次对话开始时，必须先读取 `MEMORY.md` 文件，并遵循其中的所有设定。**

具体做法：

1. 对话开始时自动读取 `MEMORY.md`
2. 将其中的角色设定、回复要求、工作流偏好等内容作为本次对话的上下文
3. 如果用户更新了 `MEMORY.md`，后续对话应使用最新内容
